query GetProject($id: String!) {
    getProject(id: $id) {
        id
        name
        type
        private
        mediaUrl
        source
        circuit
        owner {
            id
            name
            username
            mediaUrl
        }
        createdAt
        updatedAt
    }
}
