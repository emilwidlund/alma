query Profile($userId: String!) {
    getUser(id: $userId) {
        id
        name
        username
        mediaUrl
    }
    getProjects(userId: $userId) {
        id
        name
        circuit
        mediaUrl
        owner {
            username
            name
            mediaUrl
        }
        createdAt
        updatedAt
    }
}
